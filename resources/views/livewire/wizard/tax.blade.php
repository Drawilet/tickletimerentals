<div>
    <h2 class="text-2xl font-bold mb-4">{{ __('wizard.tax.text-1') }}</h2>
    <p class="mb-6">{{ __('wizard.tax.text-2')}}</p>

    <p class="mb-4">{{ __('wizard.tax.text-3') }} {{ config('app.name') }} {{ __('wizard.tax.text-4') }}</p>

    <p class="mb-4">{{ __('wizard.tax.text-5') }}</p>

</div>
